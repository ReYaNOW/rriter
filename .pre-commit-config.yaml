repos:
  - repo: local
    hooks:
      - id: api-map
        name: Generate Project Map
        entry: bash -c 'make api-map && git add PROJECT_MAP.xml'
        language: system
        pass_filenames: false